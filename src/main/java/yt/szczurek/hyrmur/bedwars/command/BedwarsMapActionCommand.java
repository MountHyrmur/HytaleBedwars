package yt.szczurek.hyrmur.bedwars.command;

//public class BedwarsMapActionCommand extends CommandBase {
public class BedwarsMapActionCommand {
    public BedwarsMapActionCommand(String literal) {
//        super(literal, "commands.bedwars.map." + literal + ".desc", false);
    }

//    @Override
//    protected void executeSync(@Nonnull CommandContext commandContext) {
//        commandContext.senderAsPlayer().getWorld().execute(() -> {
//            EventTitleUtil.showEventTitleToPlayer(commandContext.senderAsPlayer().getReference(), Message.raw("It's modded!"), Message.raw("Yeppers"), true, commandContext.senderAsPlayer().getWorld().getEntityStore().getStore());
//        });
//    }
}
